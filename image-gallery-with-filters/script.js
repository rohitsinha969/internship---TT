document.querySelector('.search-btn').onclick = functionPrompt;

function functionPrompt() {
    alert("Error 403 : server refuses to fulfill a request from the client due to authorization issues or other restrictions put in place by the API provider.");
}
